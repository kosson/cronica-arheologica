extends includes/layout-main-kosson
block content
    include includes/navbar.pug
    #wrap.container
        div.col-md-4.col-md-offset-4
            h2 Log in
            div
            form(action="/users/login", method="POST")
                p 
                    input.form-control(name="email", type="text", placeholder="adresa email")
                p 
                    input.form-control(name="password", type="password", placeholder="parola")
                button.btn.btn-lg.btn-primary.btn-block(type="submit") Log in